version: 2

sources:
  - name: staging
    database: de-capstone-project-23
    schema: globalterrorism_data
    tables:
      - name: full_raw_dataset
        description: |
          ## Overview
          The table contains global terrorism data
        columns:
          - name: eventid
            tests:
              - not_null